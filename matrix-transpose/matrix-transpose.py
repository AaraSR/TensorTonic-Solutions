import numpy as np

def matrix_transpose(A):
    """
    Return the transpose of matrix A (swap rows and columns).
    """
    # Write code here
    # A_np = np.asarray(A, dtype=float).T
    return np.asarray(A, dtype=float).T
